# Pilhas (Stacks)

Uma **pilha** é uma estrutura de dados do tipo LIFO (Last In, First Out).  
As operações principais são:

- `push(x)`: insere o elemento x no topo.  
- `pop()`: remove o elemento do topo.  

A implementação pode ser feita com **vetores** ou **listas encadeadas**.
